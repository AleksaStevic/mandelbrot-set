use crate::utils::console_log;
use std::collections::HashSet;

// static pressedKeys: HashSet<i32> = HashSet::new();

pub fn on_key_up() {}

pub fn on_key_down() {}

pub fn move_with_keyboard() {}

// static a: Closure<dyn Fn()> = Closure::<dyn Fn()>::new(move || console_log!("hello"));
